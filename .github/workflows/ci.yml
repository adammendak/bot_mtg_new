name: CI

on:
  pull_request:
  push:
    branches: [main]

# Heroku GitHub integration deploys bot-reinvented from main.

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Unit tests
        run: mvn -B test
