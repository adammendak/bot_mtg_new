<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      006-users-seed seeds password_hash with a Spring "{bcrypt}" prefix. The
      PasswordEncoder bean is a plain BCryptPasswordEncoder, which cannot parse
      that prefix, so every login failed with "Encoded password does not look
      like BCrypt".

      006 deliberately keeps the prefixed value (its checksum must match databases
      where it already ran). This changeSet strips the prefix from every existing
      row right after the seed. Idempotent: REPLACE + the LIKE filter make a
      re-run a no-op, and it also covers rows created before 006 was fixed in
      code. Portable: REPLACE / LIKE work the same on H2 and PostgreSQL.
    -->
    <changeSet id="007-strip-bcrypt-prefix" author="amendak">
        <update tableName="app_users">
            <column name="password_hash" valueComputed="REPLACE(password_hash, '{bcrypt}', '')"/>
            <where>password_hash LIKE '{bcrypt}%'</where>
        </update>
        <rollback>
            <!-- no-op: re-adding a broken prefix is never desirable -->
        </rollback>
    </changeSet>
</databaseChangeLog>
