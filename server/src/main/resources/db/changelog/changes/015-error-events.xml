<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Observability (E-5). A durable ring of "what last went wrong" — the
      scheduler catch blocks, the HTS execution gate and the scheduler watchdog
      write here so failures survive Heroku's ephemeral log. Purged after
      app.ops.error-retention-days by ErrorLog#purge.
    -->
    <changeSet id="015-error-events" author="amendak">
        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="error_events"/></not>
        </preConditions>
        <createTable tableName="error_events">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="at" type="TIMESTAMP"><constraints nullable="false"/></column>
            <column name="source" type="VARCHAR(32)"><constraints nullable="false"/></column>
            <column name="scope" type="VARCHAR(32)"/>
            <column name="detail" type="VARCHAR(64)"/>
            <column name="exception" type="VARCHAR(128)"/>
            <column name="message" type="VARCHAR(1024)"/>
            <column name="created_at" type="TIMESTAMP"><constraints nullable="false"/></column>
        </createTable>
        <createIndex tableName="error_events" indexName="idx_error_events_at">
            <column name="at"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>
