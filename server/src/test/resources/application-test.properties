app.broker=paper
app.execution-enabled=false
app.news-calendar-url=
app.webhook-urls=
spring.task.scheduling.enabled=false
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
# Non-secret dummy for automated tests only. Production uses DASHBOARD_* env vars.
app.dashboard.username=test-user
app.dashboard.password=test-password
